# My App Install Page

A free, mobile-first PWA installation landing page.

Enable GitHub Pages in repository settings and select the `main` branch. HTTPS is required for PWA installation and is provided by GitHub Pages.

Customize the app name and authorized logo in `index.html` and `manifest.webmanifest`.
